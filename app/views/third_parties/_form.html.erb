<%= form_with(model: third_party, local: true) do |form| %>
  <% if third_party.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(third_party.errors.count, "error") %> prohibited this third_party from being saved:</h2>

      <ul>
      <% third_party.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :third_party_name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address, id: :third_party_address %>
  </div>

  <div class="field">
    <%= form.label :phn_number %>
    <%= form.text_field :phn_number, id: :third_party_phn_number %>
  </div>

  <div class="field">
    <%= form.label :gst_number %>
    <%= form.text_field :gst_number, id: :third_party_gst_number %>
  </div>

  <div class="field">
    <%= form.label :due %>
    <%= form.text_field :due, id: :third_party_due %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
