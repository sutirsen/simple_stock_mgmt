<%= form_with(model: raw_material, local: true) do |form| %>
  <% if raw_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raw_material.errors.count, "error") %> prohibited this raw_material from being saved:</h2>

      <ul>
      <% raw_material.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :raw_material_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :raw_material_description %>
  </div>

  <div class="field">
    <%= form.label :qty %>
    <%= form.text_field :qty, id: :raw_material_qty %>
  </div>

  <div class="field">
    <%= form.label :unit %>
    <%= form.text_field :unit, id: :raw_material_unit %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
